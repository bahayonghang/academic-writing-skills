# Experiments

## 节目标

用案例、对比与消融支撑方法主张。标题常见 `EXPERIMENTS` / `CASE STUDY` / `CASE STUDIES`。

## 已晋升句式

- 路标中的 Experiments 紧接 Method（R003）。
- 结果句常用 `demonstrate` / `show`；较弱结论用 `indicate`（`hedge_verbs.tsv`，未全量重聚）。
- 实验节之后直接 Conclusion 的篇目很多，不必另设 Discussion。
