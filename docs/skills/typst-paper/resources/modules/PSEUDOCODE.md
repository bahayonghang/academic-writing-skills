# Pseudocode

- Prefer `algorithmic` + `algorithm-figure(...)` for IEEE-like Typst output.
- Use `lovelace` only when the user explicitly wants freer syntax.
- Keep captions, wrappers, and comments disciplined; treat line numbers as recommendations rather than hard rules.
