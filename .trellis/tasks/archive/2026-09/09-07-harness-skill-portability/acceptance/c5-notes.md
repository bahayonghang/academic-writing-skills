# C5 acceptance notes

Child task: `09-07-harness-skill-portability`. Scope: skill portable
execution, paper-audit lane provenance, CONSENSUS reading, bilingual
resource sync.

## AC map

- AC1: Six standalone `SKILL.md` files plus matching docs entry pages map
  read / search / exec / delegate onto the current session. Claude
  `allowed-tools` remains frontmatter metadata and is not claimed as
  mandatory permission on other platforms.
- AC2: paper-audit workflow, lane templates, synthesis agent, and
  editorial standards distinguish `native delegated` from
  `sequential single-agent`. Reports / `overall_assessment.txt` must
  state one of those two labels. Sequential output must not say
  `independent panel`. Sequential `CONSENSUS` is cross-perspective
  agreement in this session.
- AC3: Skill/workflow guidance keeps root-cause, academic judgment, and
  final acceptance on a strong model. Cheap-model work stays inside an
  approved file and test boundary, with escalate-on-failure. No model
  provider engine or price table.
- AC4: No scoring, gate, or JSON schema change. No fake model calls.
  Public resources, EN/ZH mirrors, and manifest hashes updated via the
  resource checker.

## Kept / unchanged

- C4 paper-audit install/sibling/limited-coverage paragraph in
  `academic-writing-skills/paper-audit/SKILL.md` remains.
- C4 install layout sections on the paper-audit docs index pages remain.
- Issue JSON field names, severity set `major|moderate|minor`,
  `floor(N/2)+1` CONSENSUS thresholds, and score cutoffs are unchanged.

## Two-mode fixture

Documentation verification only (not live multi-agent proof):
`acceptance/two-mode-review-fragments.md`.

## Evidence boundary

Five-tool live delegation stays UNVERIFIED. This C5 delivery is
standalone-package guidance plus resource sync. It is not a captured
Claude / Codex / Grok / Kimi / OMP spawn.
