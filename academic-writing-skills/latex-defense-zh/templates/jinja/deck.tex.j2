((= defense.tex skeleton for latex-defense-zh. build_deck.py escapes every value before rendering; =))
((= this template adds no escaping. Layout names and macros: references/slide-layouts.md. =))
% defense.tex: generated by latex-defense-zh scripts/build_deck.py.
% Edit slide_plan.yaml and run build_deck.py again. Do not edit this file by hand.
\documentclass[aspectratio=169,11pt]{ctexbeamer}
\usetheme{((( theme )))}
((* for package in packages *))
\usepackage{((( package )))}
((* endfor *))
\input{thesis-macros.tex}
\graphicspath{((* for path in graphicspath *)){((( path )))}((* endfor *))}

\DefenseSetup{logo={((( logo )))},stage=((( stage )))}
\title{((( title )))}
\author{((( author )))}
\DefenseSupervisor{((( supervisor )))}
\DefenseSubject{((( subject )))}
\DefenseSchool{((( school )))}
\date{((( date )))}

((* for chapter in chapters *))
\DefenseAddChapter{((( chapter.number )))}{((( chapter.title )))}
((* endfor *))

((* for item in labels *))
\DefenseDefineLabel{((( item.label )))}{((( item.number )))}
((* endfor *))

\begin{document}
((* for frame in frames *))

% defense-frame: id=((( frame.id ))) role=((( frame.role ))) chapter=((( frame.chapter ))) layout=((( frame.layout )))
((* include "frames/" ~ frame.layout ~ ".tex.j2" *))
((* endfor *))

\end{document}
