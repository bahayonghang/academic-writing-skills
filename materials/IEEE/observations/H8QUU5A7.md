---
key: H8QUU5A7
title: "Data-Driven Modeling Based on Two-Stream ${\\rm{\\lambda }}$ Gated Recurrent Unit Network With Soft Sensor Application"
venue: "IEEE Transactions on Industrial Electronics"
doi: "10.1109/TIE.2019.2927197"
item_type: journalArticle
has_pdf: true
status: degraded
pages_read: "1"
date_observed: 2026-09-11
story_pattern: pending
---

## Structure

Zotero 附件 PDF 仅 1 页：标题、作者、`Abstract—`，随后为 IEEE 关键词云，无罗马数字节标题，无 Introduction / Method / Experiments / Conclusion 正文。无法判断是否独立 Related Work。

## Openers

- abstract: `Data-driven soft` — "Data-driven soft sensors, estimating the pivotal quality variables, have been widely employed in industrial process." (p.1)

## Gap transitions

- 正文缺口句不可核。Abstract 用 `First` / `Then` / `Last` 串贡献步骤，未见 Although / However / Despite。

## Hedge verbs

- propose / causal / abstract: "This paper proposes a novel soft sensor modeling approach based on a two-stream $lambda$ gated recurrent unit (TS-$lambda$GRU) network."
- demonstrate / causal / abstract: "The application in melt viscosity index estimation for a real polymerization industrial process has demonstrated that the proposed TS-$lambda$GRUs algorithm for soft sensor modeling is more accurate and promising than other existing methods."

## Cross-section linkers

不可核。附件无节序路标。

## Candidate rules

- R001 abstract 用 `This paper proposes a novel` + 方法缩写，再用 First / Then / Last 列三步。
- R002 工业验证句用 `The application in ... has demonstrated that the proposed ... is more accurate and promising than other existing methods.`

## Candidate phrases

- `This paper proposes a novel soft sensor modeling approach based on` (abstract)
- `The application in ... has demonstrated that the proposed` (abstract)

## House style

自称是 `This paper proposes` / `the proposed TS-$lambda$GRUs algorithm`。`This paper proposes` 进 phrase_bank，不进 anti_ai_patterns。未见 `In this paper`。未见 `Here we`。

## Quotes

- p.1 abstract: Data-driven soft sensors, estimating the pivotal quality variables, have been widely employed in industrial process.
- p.1 abstract: This paper proposes a novel soft sensor modeling approach based on a two-stream $lambda$ gated recurrent unit (TS-$lambda$GRU) network.
- p.1 abstract: First, factors $lambda_1$ and $lambda_2$ are implemented to alter the linear constraint existing in the original GRU unit, enriching the information passing through.
- p.1 abstract: Then a two-stream network structure is designed, equipped with some advanced network parameter adjustment techniques such as batch normalization and dropout rate, to learn diverse features of the various process data.
- p.1 abstract: Last, the learned features from the two streams are fused and a supervised learning regression layer is employed to decrease the error between the output and label.
- p.1 abstract: The application in melt viscosity index estimation for a real polymerization industrial process has demonstrated that the proposed TS-$lambda$GRUs algorithm for soft sensor modeling is more accurate and promising than other existing methods.
