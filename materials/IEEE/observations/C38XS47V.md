---
key: C38XS47V
title: "Expensive Multiobjective Optimization by MOEA/D With Gaussian Process Model"
venue: "IEEE Transactions on Evolutionary Computation"
doi: "10.1109/TEVC.2009.2033671"
item_type: journalArticle
has_pdf: true
status: complete
pages_read: "1-19"
date_observed: 2026-09-11
story_pattern: SP-IEEE-002
---

## Structure

罗马数字标题（栏间抽取不完整，正文可核）：`I. Introduction` → 问题陈述（连续 MOP 定义，紧接 Introduction）→ `III. Algorithm Framework` → `IV. Model Building` → 候选点定位 / 选择步骤 → 实验比较（文中称 `Section VII-B`）→ Conclusion。前置 `Abstract—` 与 `Index Terms—`。无独立 Related Work。`related_work=inlined`（Introduction 中段评 EGO / ParEGO / SMS-EGO / Keane / Emmerich）。Introduction 中段给出编号特征 1)–3)，无 `The rest of this paper is organized` 全句，但有 `In Sections IV–VI, we will give and discuss Steps 3–5.`。Method 为 III–IV。Experiments 为后段测试函数比较（ZDT / LZ / DTLZ / KNO1 / VLMOP2）。

## Openers

- abstract: `This paper proposes` — "This paper proposes such a method, called MOEA/D-EGO, for dealing with expensive multiobjective optimization." (p.1)
- introduction: `IN SOME engineering` — "IN SOME engineering optimization problems, the evaluation of candidate solutions could be extremely computationally and/or financially expensive since it requires time-consuming computer simulation or expensive physical experiments." (p.1；栏首掉字)
- method: `To solve the` — "To solve the expensive MOP (1), MOEA/D with the Gaussian stochastic process model (MOEA/D-EGO), proposed in this paper, chooses a set of N weight vectors λ1,...,λN and converts approximation of the PF of (1) into N single-objective optimization problems by a decomposition method." (p.3, III.B)
- experiments: `It is evident` — "It is evident from Tables I and II that MOEA/D-EGO significantly outperforms ParEGO in terms of both the IGD and I−H metrics on ZDT1, ZDT2, ZDT3, ZDT6, and LZ08-F1." (p.12)
- conclusion: `Experimental results on` — "Experimental results on a set of test instances have shown that MOEA/D-EGO is no worse than ParEGO and SMS-EGO if the computational cost is measured by the number of function evaluations." (p.17)

## Gap transitions

- therefore (abstract): "Therefore, it is very desirable to develop methods which can generate multipler test points simultaneously." (p.1)
- therefore (introduction): "Therefore, a method is of great practical interest if it is able to produce reasonably good solutions within a given (often very tight) budget on computational cost/time." (p.1)
- however (method): "This approach can deal with nonconvex PFs but its objective function is not smooth at some points. However, it can still be used in the evolutionary algorithm framework proposed in this paper since our algorithm does not require the derivatives of the objective functions." (p.3)
- however (conclusion): "However, MOEA/D-EGO can evaluate several solutions at the same time, which makes it more suitable for solving expensive MOPs in some real-world applications." (p.17)
- therefore (conclusion): "Therefore, more effort is needed in the future to address these issues." (p.17)

## Hedge verbs

- propose / causal / abstract, introduction: "This paper proposes such a method, called MOEA/D-EGO"; "In this paper, we propose MOEA/D-EGO"
- aim / causal / introduction: "This paper aims at developing such a method for approximating the Pareto front (PF) of an expensive multiobjective optimization problem (MOP)."
- show / causal / conclusion: "Experimental results on a set of test instances have shown that MOEA/D-EGO is no worse than ParEGO and SMS-EGO"
- outperform / causal / experiments: "MOEA/D-EGO significantly outperforms ParEGO in terms of both the IGD and I−H metrics"

## Cross-section linkers

- introduction → method: "In this paper, we propose MOEA/D-EGO, MOEA/D with the Gaussian stochastic process model for expensive multiobjective optimization." 随后给出特征列表并进入 `III. Algorithm Framework` (p.2–3)
- method → experiments: "In Sections IV–VI, we will give and discuss Steps 3–5." 其后为测试函数表与 IGD 比较 (p.3, p.12)
- experiments → conclusion: 讨论 FuzzyCM / 预测质量后收束到 `Experimental results on a set of test instances have shown` (p.17)

## Candidate rules

- R001 abstract 用 `This paper proposes such a method, called` + 算法名。
- R002 Introduction 无独立 Related Work，ParEGO / SMS-EGO 评述写在引言中段。
- R003 贡献用编号列表 `The major features of MOEA/D-EGO are as follows.`
- R004 实验比较句用 `It is evident from Tables I and II that ... significantly outperforms`。
- R005 Conclusion 先 `have shown that ... is no worse than`，再用 `However` 强调可并行评估，`Therefore, more effort is needed in the future` 收局限。

## Candidate phrases

- `This paper proposes such a method, called` (abstract)
- `This paper aims at developing such a method for` (introduction)
- `In this paper, we propose` (introduction)
- `It is evident from Tables I and II that` (experiments)
- `Therefore, more effort is needed in the future to address these issues.` (conclusion)

## House style

自称是 `This paper proposes` / `This paper aims` / `In this paper, we propose` / `proposed in this paper` / `our algorithm`。未见 `Here we`。`In this paper, we propose` 与 `This paper proposes` 进 phrase_bank，不进 anti_ai_patterns。

## Quotes

- p.1 abstract: Therefore, it is very desirable to develop methods which can generate multipler test points simultaneously.
- p.1 abstract: This paper proposes such a method, called MOEA/D-EGO, for dealing with expensive multiobjective optimization.
- p.1 abstract: Extensive experimental studies have been carried out to investigate the ability of the proposed algorithm.
- p.1 introduction: IN SOME engineering optimization problems, the evaluation of candidate solutions could be extremely computationally and/or financially expensive since it requires time-consuming computer simulation or expensive physical experiments.
- p.1 introduction: Therefore, a method is of great practical interest if it is able to produce reasonably good solutions within a given (often very tight) budget on computational cost/time.
- p.1 introduction: This paper aims at developing such a method for approximating the Pareto front (PF) of an expensive multiobjective optimization problem (MOP).
- p.2 introduction: In this paper, we propose MOEA/D-EGO, MOEA/D with the Gaussian stochastic process model for expensive multiobjective optimization.
- p.2 introduction: Due to the parallel nature of MOEA/D, MOEA/D-EGO is able to generate several test points to evaluate in a batch way, which makes it suitable in parallel computing environments.
- p.3 method: To solve the expensive MOP (1), MOEA/D with the Gaussian stochastic process model (MOEA/D-EGO), proposed in this paper, chooses a set of N weight vectors λ1,...,λN and converts approximation of the PF of (1) into N single-objective optimization problems by a decomposition method.
- p.3 method: This approach can deal with nonconvex PFs but its objective function is not smooth at some points. However, it can still be used in the evolutionary algorithm framework proposed in this paper since our algorithm does not require the derivatives of the objective functions.
- p.12 experiments: It is evident from Tables I and II that MOEA/D-EGO significantly outperforms ParEGO in terms of both the IGD and I−H metrics on ZDT1, ZDT2, ZDT3, ZDT6, and LZ08-F1.
- p.17 conclusion: Experimental results on a set of test instances have shown that MOEA/D-EGO is no worse than ParEGO and SMS-EGO if the computational cost is measured by the number of function evaluations.
- p.17 conclusion: However, MOEA/D-EGO can evaluate several solutions at the same time, which makes it more suitable for solving expensive MOPs in some real-world applications.
- p.17 conclusion: Therefore, more effort is needed in the future to address these issues.
