# 设计

分层验收表直接记录实际证据，不新增采集服务、持久化 schema 或评分系统。项目 spec 是本次选择的团队知识回写位置，公开指南解释适用工具。本轮规划交付完成不等于此实施子任务完成。

推荐实施验收仅含静态规则、隔离脚本与诚实完整台账；AC1–AC4 全通过后可关闭该交付范围。真实五平台新会话/委派是可选扩展，不获授权或环境缺失时记录 UNVERIFIED，不将其冒充完成，也不把它当本次静态范围的隐藏阻断。

## 文件与边界

具体文件范围见本任务 prd.md；实现只读取本范围所需上下文。共享文件按前置顺序串行处理。相关根因见父任务 research/runtime-findings.md、test-baseline.md 及 harness-rules.md。

## 模型与工具

由强模型独立审查并汇总。便宜模型可整理命令输出与文件清单，不可给 UNVERIFIED 项盖章或代替作者决策。

## 回退

只回退本任务引入的 diff，保留原有 README 推荐模型改动。源资源、镜像与 manifest 同进同退；不回滚其他子任务。
