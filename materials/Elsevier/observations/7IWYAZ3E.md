---
key: 7IWYAZ3E
title: "Multivariate time series prediction of complex systems based on graph neural networks with location embedding graph structure learning"
venue: "Advanced Engineering Informatics"
doi: "10.1016/j.aei.2022.101810"
item_type: journalArticle
has_pdf: true
status: complete
pages_read: "1-12"
date_observed: 2026-09-11
story_pattern: SP-ELS-001
---

## Structure

数字节：`1. Introduction` → `2. Background` → `3. Methodology` → `5. Experiments` → `6. Conclusions`。前置 `ABSTRACT` 与 `ARTICLE INFO` / `Keywords`。独立 Related Work：`2. Background`（GNN 多元时序、图卷积、图结构学习）。Introduction 已有 PFPP 软测量与故障检测文献，末为编号改进 `(1)` / `(2)` / `(3)` + 节序路标。Method 含 LPGCN 与位置嵌入 GSL。Experiments 标题为 `5. Experiments`（XFM_V210）。

## Openers

- abstract: `Graph convolutional neural` — "Graph convolutional neural networks (GNNs) have an excellent expression ability for complex systems."
- introduction: `This paper explores` — "This paper explores the possibility of applying graph neural networks in complex systems of fiber production industry."
- related_work: `The application of` — "The application of graph neural network to multivariate time series forecasting has received extensive attention recently."
- method: `Several studies based` — "Several studies based on physics-informed graph neural networks have shown that current graph neural networks are only suitable for one class of data generated by network heat diffusion dynamics."
- experiments: `The proposed method will` — "The proposed method will be applied in the polyester fiber polymerization process."
- conclusion: `This paper mainly` — "This paper mainly focuses on the application of graph neural networks to complex dynamical networks."

## Gap transitions

- however (abstract): "However, the smoothing hypothesis based GNNs have certain limitations for complex process industrial systems with high dynamics and noisy environment."
- in addition (abstract): "In addition, it is difficult to obtain an accurate information about the interconnections of sensor networks in manufacturing systems, which brings challenges to the application of GNNs."
- however (introduction): "However, they do not consider the order information of the time series, and the model framework does not have an efficient way to learn the graph adjacency matrix."
- therefore (introduction): "Therefore, it is necessary to propose an end-to-end graph structure learning method and design a spatiotemporal model framework suitable for PFPP."

## Hedge verbs

- introduces / causal / abstract: "This paper introduces a graph convolution filter with a serial alternating structure of low-pass filter and high-pass filter"
- propose / causal / abstract, introduction: "we propose a simple and effective method to learn graph structure information during training"; "We propose an alternative filtering method"
- demonstrate / associative / abstract: "the results demonstrate the effectiveness of the model"
- proposed / causal / conclusion: "This paper proposed to use a high-pass filter and a low-pass filter interlaced graph convolution layers"

## Cross-section linkers

- introduction → background: "The rest of the paper is organized as follows: Section 2 reviews the researches on multivariate time series forecasting using GNNs and summarizes some progress in joint graph structure learning."
- background → method: "After that, in the third section, a low-frequency preserved graph convolution method is proposed."
- method → experiments: "The fifth section verifies the effectiveness of the proposed prediction framework through experiments on real process industry datasets."
- experiments → conclusion: 消融表后 `6. Conclusions`

## Candidate rules

- R001 独立 Related Work：`2. Background`（位于 Introduction 之后、Methodology 之前）。
- R003 节序路标：`The rest of the paper is organized as follows`
- R004 编号贡献：Introduction 末 `(1)` / `(2)` / `(3)`
- R009 自称：`This paper introduces` / `we propose` / `This paper proposed`

## Candidate phrases

- `This paper introduces a graph convolution filter` (abstract)
- `Furthermore, we propose a simple and effective method` (abstract)
- `The rest of the paper is organized as follows` (introduction)
- `The experimental results are shown in Table 2` (experiments)
- `This paper proposed to use a high-pass filter and a low-pass filter interlaced graph convolution layers` (conclusion)

## House style

自称 `This paper introduces` / `we propose` / `This paper proposed` / `This paper mainly focuses`。第一人称复数与 `this paper` 并用。进 phrase_bank，不进 anti_ai_patterns。

## Quotes

- abstract: Graph convolutional neural networks (GNNs) have an excellent expression ability for complex systems.
- abstract: However, the smoothing hypothesis based GNNs have certain limitations for complex process industrial systems with high dynamics and noisy environment.
- abstract: This paper introduces a graph convolution filter with a serial alternating structure of low-pass filter and high-pass filter to alleviate the problem of node feature loss.
- abstract: Furthermore, we propose a simple and effective method to learn graph structure information during training.
- introduction: This paper explores the possibility of applying graph neural networks in complex systems of fiber production industry.
- introduction: The rest of the paper is organized as follows: Section 2 reviews the researches on multivariate time series forecasting using GNNs and summarizes some progress in joint graph structure learning.
- related_work: The application of graph neural network to multivariate time series forecasting has received extensive attention recently.
- experiments: The proposed method will be applied in the polyester fiber polymerization process.
- experiments: It can be seen that our model exceeds the state of the art model in all prediction durations.
- conclusion: This paper mainly focuses on the application of graph neural networks to complex dynamical networks.
- conclusion: This paper proposed to use a high-pass filter and a low-pass filter interlaced graph convolution layers to approximate an ideal low-pass filter, which slow down the rapid contraction of low-frequency components.
