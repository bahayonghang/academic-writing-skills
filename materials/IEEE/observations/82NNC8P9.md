---
key: 82NNC8P9
title: "Hierarchical Canonical Correlation Analysis With Application to Process Monitoring"
venue: "IEEE Transactions on Industrial Informatics"
doi: "10.1109/TII.2025.3567402"
item_type: journalArticle
has_pdf: true
status: complete
pages_read: "1-11"
date_observed: 2026-09-11
story_pattern: SP-IEEE-002
---

## Structure

罗马数字标题：`I. INTRODUCTION` → `II. DEEP MULTIVARIATE STATISTICAL MODEL` → `III. FAULT DETECTION BASED ON HCCA` → `IV. CASE STUDY` → `V. CONCLUSION`。前置 `Abstract—` 与 `Index Terms—`。无独立 Related Work。`related_work=inlined`（Introduction 中段评 CCA 变体、KCCA、DeCCA、deep PLS / deep PCA-ICA）。Introduction 末为编号贡献与 `The rest of this article is organized as follows`。Method 标题为 `DEEP MULTIVARIATE STATISTICAL MODEL`。III 给监测逻辑。Experiments 标题为 `CASE STUDY`（TE + WWTP）。附录给缩写表。

## Openers

- abstract: `The idea of` — "The idea of stacking layers is adopted to construct a deep multivariate statistical model, hierarchical canonical correlation analysis (HCCA)." (p.1)
- introduction: `LARGE amounts of` — "LARGE amounts of data are collected with the rapid development of sensor technology." (p.1)
- method: `Deep learning implements` — "Deep learning implements a hierarchical structure, which is one of the key characteristics." (p.2, II.A)
- experiments: `TE process is` — "TE process is a benchmark that is now widely used to validate monitoring methods [31]." (p.6, IV.A)
- conclusion: `HCCA is proposed` — "HCCA is proposed to improve the data feature extraction capability, further HCCA-based fault detection is given." (p.9)

## Gap transitions

- however (introduction): "However, the above CCA-based statistical methods assume a linear relationship between variables and perform poorly when confronted with nonlinearity." (p.1)
- although (introduction): "Although KCCA can capture the nonlinear relationships, how to select the appropriate kernel function still is a puzzle." (p.1)
- despite (introduction): "Despite the successful combination of CCA and DNN, the model does not sufficiently extract the data inherent features." (p.1)
- based on (introduction): "Based on this thinking, Kong and Ge [23] proposed the lightweight depth model, deep partial least square (PLS)." (p.1)

## Hedge verbs

- propose / causal / abstract, introduction: "The HCCA-based fault monitoring method is proposed"; "The HCCA model is proposed from the idea of multilayers stacking"
- demonstrate / causal / introduction: "Its correlation advantages are theoretically demonstrated"
- show / causal / abstract: "The experiment results on Tennessee Eastman and real industry wastewater treatment processes show an average fault detection rate of 87.91% and 99.49%."
- indicate / causal / experiments: "It indicates that HCCA is more effective in WWTP."

## Cross-section linkers

- introduction → method: "The rest of this article is organized as follows. HCCA model and its layer correlation are derived in Section II. HCCA based process monitoring method is given in Section III. Two industrial experiments are demonstrated in Section IV. Finally, Section V concludes this article." (p.2)
- method → detection: 复杂度指标后 `III. FAULT DETECTION BASED ON HCCA` (p.5)
- detection → experiments: 贡献分解后 `IV. CASE STUDY` (p.6)
- experiments → conclusion: 根因解释后 `V. CONCLUSION` (p.9)

## Candidate rules

- R001 abstract 用被动 `is adopted` / `is proposed`，不用 `Here we`。
- R002 Introduction 无独立 Related Work；CCA/KCCA/DeCCA 评述写在引言中段。
- R003 Introduction 末用 `The rest of this article is organized as follows` 指向 II–V。
- R004 Experiments 标题为 `CASE STUDY`。
- R005 Conclusion 用被动 `HCCA is proposed`，再用 `there are still several important issues for further investigating` 指向后续。

## Candidate phrases

- `The HCCA-based fault monitoring method is proposed for industrial processes` (abstract)
- `The main contributions are as follows.` (introduction)
- `The rest of this article is organized as follows.` (introduction)
- `HCCA is proposed to improve the data feature extraction capability` (conclusion)
- `there are still several important issues for further investigating` (conclusion)

## House style

自称是 `the proposed HCCA model` / `this article` / 被动 `is proposed`。少用 we。未见 `Here we`。`The rest of this article is organized as follows` 进 phrase_bank，不进 anti_ai_patterns。未见 `In this paper`。

## Quotes

- p.1 abstract: The idea of stacking layers is adopted to construct a deep multivariate statistical model, hierarchical canonical correlation analysis (HCCA).
- p.1 abstract: The HCCA-based fault monitoring method is proposed for industrial processes, and the variable contributions are analyzed based on the residual statistic.
- p.1 abstract: The experiment results on Tennessee Eastman and real industry wastewater treatment processes show an average fault detection rate of 87.91% and 99.49%.
- p.1 introduction: LARGE amounts of data are collected with the rapid development of sensor technology.
- p.1 introduction: However, the above CCA-based statistical methods assume a linear relationship between variables and perform poorly when confronted with nonlinearity.
- p.1 introduction: Despite the successful combination of CCA and DNN, the model does not sufficiently extract the data inherent features.
- p.2 introduction: The main contributions are as follows.
- p.2 introduction: The rest of this article is organized as follows. HCCA model and its layer correlation are derived in Section II. HCCA based process monitoring method is given in Section III. Two industrial experiments are demonstrated in Section IV. Finally, Section V concludes this article.
- p.2 method: Deep learning implements a hierarchical structure, which is one of the key characteristics.
- p.5 method: The HCCA model characterizes the data correlation and is used in many areas, such as feature fusion, data degradation, and process monitoring.
- p.6 experiments: TE process is a benchmark that is now widely used to validate monitoring methods [31].
- p.6 experiments: As a whole, HCCA is superior to CCA. The average fault detection rate (FDR) of HCCA increased by 7.19% and the false alarm rate (FAR) decreased by 1.52%.
- p.9 conclusion: HCCA is proposed to improve the data feature extraction capability, further HCCA-based fault detection is given.
- p.9 conclusion: Despite the satisfactory performance of the HCCA on fault monitoring, there are still several important issues for further investigating.
