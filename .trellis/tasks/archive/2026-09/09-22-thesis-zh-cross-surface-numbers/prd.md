# C4：有证据绑定的跨表面数字候选

## Goal / Dependencies
承担父任务对应主题需求与通用约束；C3后执行以串行移交公开文件，不消费C1治理文件。
用户2026-09-22确认：能明确判定的数字做opt-in，歧义保留NEEDS-LLM。

## Confirmed Facts
academic-writing-skills/latex-thesis-zh/scripts/analyze_experiment.py:430 已有RA区间，
第518行已有段落切分；RA九码没有跨表面绑定。
父research/planning-evidence记录来源三表面规则；“章节任意同值”不能推出“同口径同指标”。

## Requirements
- R1: --cross-surface扫描同章结果表、正文与本章小结，保留来源；仅支持已声明简单表格与明确引用。
- R2: 相同表格标签、指标、对象/方法、评价集、单位才允许比较终值。
  不设相近容差，不以相同裸数字替代绑定；不能唯一绑定则NEEDS-LLM。
- R3: 已绑定记录不同终值、小结记录在表缺失、被正文/小结声明为关键结果而缺一表面分别给候选；
  不要求表的每个单元格均重复进正文小结；三处一致只说明支持子集一致。
- R4: 同表评价集混用、不同名指标互推只定位自然语言候选，不判断真实等价或修正终值。
  配置仅指标/评价集词表，不建设指标规则语言。
- R5: 指南补三表面人工核读、显示层复算与源层重算区分、分母/单位/对象和不同名指标边界。
- R6: 绑定反例、缺表面、未覆盖、旧RA/默认独立以及词表错误有测试；文档镜像/规范/eval同步。

## Acceptance Criteria
- [ ] AC1 (R1,R2,R3): 支持表格上同对象三处一致零差异；92.1与92.3同键报差异；
  别的指标/方法碰巧92.1不能抵消差异；缺表值或缺正文/小结分别报候选。
- [ ] AC2 (R2,R3): 指标/对象/评价集/单位任一不明、多个表候选、复杂宏表、区间口径不清均NEEDS-LLM；
  无结果表/无小结不能伪装为三处一致；默认不开启不新增覆盖信息。
- [ ] AC3 (R4): 明确同表两个评价集名、由指标A换算为B各有候选及否定句反例；
  自定义词表实际改变识别，非法输入非零退出。
- [ ] AC4 (R5,R6): 指南有显示复算/分母与不同名指标正反例；新候选均不输出修正数字；
  默认及旧九码回归不变，父design门禁通过、公开无私有数字/专名。

## Out of Scope
不读CSV/实验结果，不复算比例、不自动改数、不跨章、不判断真实同义指标；
不添加持久化IR或通用表格框架，不判语义未闭合的“相近数值”。
旧编号映射：R4.1→R1/R2/R3；R4.2/R4.3→R4；R4.4→R5；R4.5→R6。
