# Pseudocode

- Prefer `figure` + `algorithmicx` / `algpseudocodex` for IEEE submissions.
- Do not default to floating `algorithm` / `algorithm2e` in IEEE papers.
- Use a direct figure caption and reference the block before it appears.
- Treat line numbers and explicit input/output markers as recommendations unless the venue says otherwise.
